# Copy to .env for local data refresh (never commit .env).
VR_TOKEN=
VR_WALLET_ADDRESS=0x0000000000000000000000000000000000000000

# Optional if Omni blocks datacenter IPs (e.g. GitHub Actions):
# HTTPS_PROXY=https://user:pass@host:port
